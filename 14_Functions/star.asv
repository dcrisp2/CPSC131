function [] = star( points )
    %points = 5;
    % For 5 point star
    %THETA = pi/2:0.8*pi:(pi/2)+(points)*0.8*pi;
    Start=pi
    THETA = pi(1/2):pi(2/3):pi*(1+(2/3)*points);
    
    RHO = ones(1,points+1);
    
    polar(THETA,RHO);
end