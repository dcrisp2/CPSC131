function [ pival ] = pival_carlo( midx, midy, N, R )
    %pival_carlo Does monte carlo simulation to estimate the value of pi
    
    x = 2*R*((midx - 0.5)+rand(N,1));
    y = 2*R*((midy - 0.5)+rand(N,1));
    figure('color','white');
    
    radius = sqrt(x.^2+y1.^2);
    m = 0;
    for i=1:n
        if radius(i)<=R
            m=m+1;
            plot(x(i),y(i),'b.');
        else
            plot(x(i),y(i),'r.');
        end
    end
    m/ra


end
